# ============================================================
# Alacritty - macOS Specific Configuration
# ============================================================

import:
  - alacritty.yml

# Override shell path for macOS (Homebrew)
shell:
  program: /opt/homebrew/bin/fish
  args:
    - -l
    - -c
    - "tmux new-session -A -s main"

# macOS-specific window settings (optional)
# window:
#   decorations: buttonless
#   option_as_alt: Both
