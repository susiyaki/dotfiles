# ============================================================
# Alacritty - Linux Specific Configuration
# ============================================================

[general]
import = ["alacritty-base.toml"]

# Override shell path for Linux (Arch Linux with Nix)
[terminal.shell]
program = "/usr/bin/fish"  # Arch Linux system fish
args = ["-l", "-c", "tmux new-session -A -s main"]
